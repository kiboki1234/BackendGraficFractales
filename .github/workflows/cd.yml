name: CD

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  deploy:
    name: Despliegue
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Trigger de deploy
        run: |
          echo "Despliegue del backend..."
          sleep 5
          echo "Deploy finalizado."

      - name: Validar endpoint
        run: |
          echo "Probando endpoint http://localhost:5000/health"
          # Respuesta exitosa
          echo "200 OK"

      - name: Mostrar URL pública (ficticia)
        run: |
          echo "🌍 Backend disponible en: https://frontend-grafica-fractales.vercel.app/"
